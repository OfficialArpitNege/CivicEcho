rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes in development (emulator mode)
    // These rules MUST be updated for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
